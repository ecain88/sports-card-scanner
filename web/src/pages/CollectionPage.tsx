export function CollectionPage() {
  return (
    <div className="page">
      <h1>Collection</h1>
      <div className="card">
        <p className="muted">
          Saved cards will appear here once a Convex deployment URL is configured. Each scan saves
          its centering result and eBay query so you can revisit comps anytime.
        </p>
      </div>
    </div>
  );
}
